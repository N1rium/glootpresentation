import React from 'react';
import styled from 'styled-components';
import Slide from '../../slide';

const Start = styled(Slide)`
  background: red;
`;

export default () => {
  return <Start></Start>;
};
