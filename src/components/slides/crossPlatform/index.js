import React from 'react';
import styled from 'styled-components';
import Slide from '../../slide';

const CrossPlatform = styled(Slide)`
  background: yellow;
`;

export default () => {
  return <CrossPlatform></CrossPlatform>;
};
